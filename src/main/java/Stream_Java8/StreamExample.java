package Stream_Java8;

public class StreamExample {
    public static void main(String[] args) {
/* Stream can be created from Collections, Lists, Sets, ints, longs, doubles, arrays, lines of file
*Stream operations are either intermediate or terminal
*intermediate operations such as filter, map or sort return a stream so we can chain multiple intermediate operations
*terminal operations such as forEach, collect, are either void or return a non stream-result
*
 * */
    }
}
